# knowledge-service/src/main/resources/bootstrap.yml
spring:
  application:
    name: knowledge-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
