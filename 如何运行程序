首先，要改动配置文件和建立数据库表。

请将qa-service模块中的application.yml,将其中的deepseek的api key替换为自己的。

若此前没有申请过api key请从https://platform.deepseek.com/api_keys申请，且需要充值使用

数据库sql语句已在文件中给出,数据库配置根据自己实际在application.yml文件中修改。

其它配置文件如nacos可根据自己需要修改，打开bin文件cmd运行，输入命令startup.cmd -m standalone。

然后依次启动后端微服务，打开nacos看是否已经注册到服务列表里面。

前端文件为fronted-vue3，启动命令npm run dev.

已经部署到了公网上，想直接看演示效果的可访问http://120.27.203.154:8282 